/** @type {import("prettier").Config} */
const config = {
    arrowParens: 'always',
    bracketSpacing: false,
    printWidth: 80,
    semi: false,
    singleQuote: true,
    tabWidth: 4,
    trailingComma: 'es5',
    useTabs: false,
}

module.exports = config
